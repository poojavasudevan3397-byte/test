Attached HTML and CSS Context

<div draggable="false" class="dvn-scroller stDataFrameGlideDataEditor" style="width: 674px; height: 398px; cursor: default;"><div class="dvn-scroll-inner dvn-hidden"><div class="dvn-stack"><div style="width: 517px; height: 0px;"></div><div style="width: 0px; height: 735px;"></div></div></div></div>


/* Inline style */
element {
  width: 674px;
  height: 398px;
  cursor: default;
  width: 674px;
  height: 398px;
  cursor: default;
}

/* Matched Rule from user-agent */
div {
  display: block;
}

/* Matched Rule from user-agent */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Matched Rule from regular */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Matched Rule from regular */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Matched Rule from regular */
.st-emotion-cache-12yo0lz .stDataFrameGlideDataEditor {
  height: 100%;
  min-width: 100%;
  min-height: 72px;
  border-radius: 0.5rem;
  height: 100%;
  min-width: 100%;
  min-height: 72px;
  border-top-left-radius: 0.5rem;
  border-top-right-radius: 0.5rem;
  border-bottom-right-radius: 0.5rem;
  border-bottom-left-radius: 0.5rem;
}

/* Matched Rule from regular */
.st-emotion-cache-12yo0lz .dvn-scroller {
  overflow: auto !important;
  overflow-x: auto !important;
  overflow-y: auto !important;
}

/* Matched Rule from regular */
.gdg-s1dgczr6 .dvn-scroller {
  overflow: var(--s1dgczr6-0);
  transform: translateZ(0);
  transform: translateZ(0px);
}

/* Inherited from ancestor level 1 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 1 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 1 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 1 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 2 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 2 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 2 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 2 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 2 (regular) */
.gdg-wmyidgi>:first-child {
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  position: absolute;
  left: 0px;
  top: 0px;
  width: 100%;
  height: 100%;
}

/* Inherited from ancestor level 3 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 3 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 3 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 3 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 3 (regular) */
.gdg-wmyidgi {
  position: relative;
  min-width: 10px;
  min-height: 10px;
  max-width: 100%;
  max-height: 100%;
  width: var(--wmyidgi-0);
  height: var(--wmyidgi-1);
  overflow: hidden;
  overflow: clip;
  direction: ltr;
  position: relative;
  min-width: 10px;
  min-height: 10px;
  max-width: 100%;
  max-height: 100%;
  width: var(--wmyidgi-0);
  height: var(--wmyidgi-1);
  overflow-x: clip;
  overflow-y: clip;
  direction: ltr;
}

/* Inherited from ancestor level 3 (regular) */
.st-emotion-cache-12yo0lz .stDataFrameGlideDataEditor {
  height: 100%;
  min-width: 100%;
  min-height: 72px;
  border-radius: 0.5rem;
  height: 100%;
  min-width: 100%;
  min-height: 72px;
  border-top-left-radius: 0.5rem;
  border-top-right-radius: 0.5rem;
  border-bottom-right-radius: 0.5rem;
  border-bottom-left-radius: 0.5rem;
}

/* Inherited from ancestor level 4 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 4 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 4 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 4 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 5 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 5 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 5 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 5 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 5 (regular) */
.st-emotion-cache-12yo0lz {
  position: relative;
  display: inline-block;
  height: 100%;
  position: relative;
  display: inline-block;
  height: 100%;
}

/* Inherited from ancestor level 6 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 6 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 6 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 6 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 6 (regular) */
.st-emotion-cache-uwwqev {
  width: 100%;
  height: 100%;
  width: 100%;
  height: 100%;
}

/* Inherited from ancestor level 7 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 7 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 7 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 7 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 7 (regular) */
.st-emotion-cache-1vo6xi6 {
  width: 100%;
  height: auto;
  max-width: 100%;
  min-width: 1rem;
  position: relative;
  overflow: visible;
  width: 100%;
  height: auto;
  max-width: 100%;
  min-width: 1rem;
  position: relative;
  overflow-x: visible;
  overflow-y: visible;
}

/* Inherited from ancestor level 8 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 8 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 8 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 8 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 8 (regular) */
.st-emotion-cache-tn0cau {
  display: flex;
  gap: 1rem;
  width: 100%;
  max-width: 100%;
  height: auto;
  min-width: 1rem;
  flex-flow: column;
  flex: 1 1 0%;
  -webkit-box-align: stretch;
  align-items: stretch;
  -webkit-box-pack: start;
  justify-content: start;
  display: flex;
  row-gap: 1rem;
  column-gap: 1rem;
  width: 100%;
  max-width: 100%;
  height: auto;
  min-width: 1rem;
  flex-direction: column;
  flex-grow: 1;
  flex-shrink: 1;
  flex-basis: 0%;
  -webkit-box-align: stretch;
  align-items: stretch;
  -webkit-box-pack: start;
  justify-content: start;
  flex-wrap: nowrap;
}

/* Inherited from ancestor level 9 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 9 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 9 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 9 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 9 (regular) */
.st-aq {
  padding-left: 0px;
  padding-left: 0px;
}

/* Inherited from ancestor level 9 (regular) */
.st-b5 {
  outline: none;
  outline-color: initial;
  outline-style: none;
  outline-width: initial;
}

/* Inherited from ancestor level 9 (regular) */
.st-bh {
  padding-right: 0px;
  padding-right: 0px;
}

/* Inherited from ancestor level 9 (regular) */
.st-bi {
  padding-bottom: 0px;
  padding-bottom: 0px;
}

/* Inherited from ancestor level 9 (regular) */
.st-co {
  -webkit-box-flex: 1;
  flex-grow: 1;
  -webkit-box-flex: 1;
  flex-grow: 1;
}

/* Inherited from ancestor level 9 (regular) */
.st-ew {
  padding-top: 1rem;
  padding-top: 1rem;
}

/* Inherited from ancestor level 10 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 10 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 10 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 10 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 10 (regular) */
.st-f5 {
  transform: none;
  transform: none;
}

/* Inherited from ancestor level 11 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 11 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 11 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 11 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 11 (regular) */
.st-emotion-cache-8atqhb {
  width: 100%;
  width: 100%;
}

/* Inherited from ancestor level 12 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 12 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 12 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 12 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 12 (regular) */
.st-emotion-cache-tn0cau {
  display: flex;
  gap: 1rem;
  width: 100%;
  max-width: 100%;
  height: auto;
  min-width: 1rem;
  flex-flow: column;
  flex: 1 1 0%;
  -webkit-box-align: stretch;
  align-items: stretch;
  -webkit-box-pack: start;
  justify-content: start;
  display: flex;
  row-gap: 1rem;
  column-gap: 1rem;
  width: 100%;
  max-width: 100%;
  height: auto;
  min-width: 1rem;
  flex-direction: column;
  flex-grow: 1;
  flex-shrink: 1;
  flex-basis: 0%;
  -webkit-box-align: stretch;
  align-items: stretch;
  -webkit-box-pack: start;
  justify-content: start;
  flex-wrap: nowrap;
}

/* Inherited from ancestor level 13 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 13 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 13 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 13 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 13 (regular) */
.st-emotion-cache-zy6yx3 {
  width: 100%;
  padding: 6rem 1rem 10rem;
  max-width: initial;
  min-width: auto;
  width: 100%;
  padding-left: 1rem;
  padding-right: 1rem;
  padding-top: 6rem;
  padding-bottom: 10rem;
  max-width: initial;
  min-width: auto;
}

/* Inherited from ancestor level 14 (user-agent) */
article, aside, footer, header, hgroup, main, nav, search, section {
  display: block;
}

/* Inherited from ancestor level 14 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 14 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 14 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 14 (regular) */
.st-emotion-cache-4rsbii {
  display: flex;
  flex-direction: column;
  width: 100%;
  overflow: auto;
  -webkit-box-align: center;
  align-items: center;
  height: 100dvh;
  display: flex;
  flex-direction: column;
  width: 100%;
  overflow-x: auto;
  overflow-y: auto;
  -webkit-box-align: center;
  align-items: center;
  height: 100dvh;
}

/* Inherited from ancestor level 14 (regular) */
.st-emotion-cache-4rsbii {
  position: absolute;
  inset: 0px;
  height: 100dvh;
  position: absolute;
  top: 0px;
  left: 0px;
  right: 0px;
  bottom: 0px;
  height: 100dvh;
}

/* Inherited from ancestor level 15 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 15 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 15 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 15 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 15 (regular) */
.st-emotion-cache-6px8kg {
  width: 100%;
  min-width: 0px;
  height: 100dvh;
  width: 100%;
  min-width: 0px;
  height: 100dvh;
}

/* Inherited from ancestor level 16 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 16 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 16 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 16 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 16 (regular) */
.st-emotion-cache-1yiq2ps {
  display: flex;
  flex-direction: row;
  -webkit-box-pack: start;
  place-content: flex-start;
  -webkit-box-align: stretch;
  align-items: stretch;
  position: absolute;
  inset: 0px;
  overflow: hidden;
  display: flex;
  flex-direction: row;
  -webkit-box-pack: start;
  justify-content: flex-start;
  -webkit-box-align: stretch;
  align-items: stretch;
  align-content: flex-start;
  position: absolute;
  top: 0px;
  left: 0px;
  right: 0px;
  bottom: 0px;
  overflow-x: hidden;
  overflow-y: hidden;
}

/* Inherited from ancestor level 17 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 17 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 17 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 17 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 17 (regular) */
.st-emotion-cache-p7p7gf {
  position: absolute;
  background: rgb(255, 255, 255);
  color: rgb(38, 39, 48);
  inset: 0px;
  color-scheme: light;
  overflow: hidden;
  position: absolute;
  background-image: initial;
  background-position-x: initial;
  background-position-y: initial;
  background-size: initial;
  background-repeat: initial;
  background-attachment: initial;
  background-origin: initial;
  background-clip: initial;
  background-color: rgb(255, 255, 255);
  color: rgb(38, 39, 48);
  top: 0px;
  left: 0px;
  right: 0px;
  bottom: 0px;
  color-scheme: light;
  overflow-x: hidden;
  overflow-y: hidden;
}

/* Inherited from ancestor level 18 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 18 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 18 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 18 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 19 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 19 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 19 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 19 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 20 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 20 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 20 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 20 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 21 (user-agent) */
body {
  display: block;
  margin-top: 8px;
  margin-right: 8px;
  margin-bottom: 8px;
  margin-left: 8px;
}

/* Inherited from ancestor level 21 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 21 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 21 (regular) */
body {
  counter-reset: katexEqnNo mmlEqnNo;
  counter-reset: katexEqnNo 0 mmlEqnNo 0;
}

/* Inherited from ancestor level 21 (regular) */
body {
  margin: 0px;
  font-family: "Source Sans", sans-serif;
  font-weight: 400;
  line-height: 1.6;
  color: rgb(38, 39, 48);
  background-color: rgb(255, 255, 255);
  text-size-adjust: 100%;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
  -webkit-font-smoothing: auto;
  margin-top: 0px;
  margin-right: 0px;
  margin-bottom: 0px;
  margin-left: 0px;
  font-family: "Source Sans", sans-serif;
  font-weight: 400;
  line-height: 1.6;
  color: rgb(38, 39, 48);
  background-color: rgb(255, 255, 255);
  text-size-adjust: 100%;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
  -webkit-font-smoothing: auto;
}

/* Inherited from ancestor level 22 (user-agent) */
html {
  display: block;
}

/* Inherited from ancestor level 22 (user-agent) */
:root {
  view-transition-name: root;
}

/* Inherited from ancestor level 22 (regular) */
*, ::before, ::after {
  box-sizing: border-box;
  box-sizing: border-box;
}

/* Inherited from ancestor level 22 (regular) */
* {
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
  scrollbar-width: thin;
  scrollbar-color: transparent transparent;
}

/* Inherited from ancestor level 22 (regular) */
html {
  font-size: 16px;
  font-size: 16px;
}
